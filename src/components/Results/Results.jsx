import React from "react";

import classes from "./Results.module.css";

const Results = () => {
  return <section className={`${classes.results} ${classes.hidden}`}></section>;
};

export default Results;
